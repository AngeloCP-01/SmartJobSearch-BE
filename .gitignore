node_modules/
uploads/
.env
.env.local
dist/
build/
*.log
.DS_Store
.env.test
.env.prod

# Local résumé sample artifacts for manual extractRich testing (not source)
docs/*.docx
docs/*.pdf

# SDD scratch (briefs/reports/review packages/progress ledger)
.superpowers/

# Eval run output. Recordings ARE committed (they make --replay reproducible
# for anyone), but a scored run is a point-in-time measurement, not source.
evals/results/
