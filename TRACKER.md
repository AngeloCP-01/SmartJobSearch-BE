# Backend Tracker (v1)

Status legend: ☐ Not started · ◐ In progress · ☑ Done · ⛔ Blocked

**Last updated:** 2026-06-30
**Master tracker:** `../TRACKER.md`

| ID | Milestone | Status | Notes |
|----|-----------|--------|-------|
| BE-0 | Scaffold | ☑ | Express + Prisma + Docker Postgres (host port 5434), health check |
| BE-1 | Auth | ☑ | register/login/refresh/logout/me; access + refresh (httpOnly cookie) with rotation |
| BE-2 | Companies | ☑ | CRUD + search, per-user isolation |
| BE-3 | Applications + Kanban API | ☑ | CRUD + `PATCH /:id/status` |
| BE-4 | Interviews | ☑ | CRUD linked to applications |
| BE-5 | Dashboard | ☑ | `GET /dashboard/summary` |
| BE-6 | Deploy | ☑ | **Live** 2026-06-25 — Render (free web service) `https://smartjobsearch-api.onrender.com/api` + Neon Postgres + Supabase Storage (S3). `render.yaml` blueprint; uploads via `STORAGE_DRIVER=s3`. See `DEPLOY.md`. |

## v1.5 — Application Details (backend slice) ☑ (2026-06-23, merged to `main`)
Application responses now include `company { id, name }`; `PATCH /applications/:id` with `companyId: null` unlinks. (Frontend drawer lives in the FE repo.)

## v2 — Contacts (backend slice) ☑ (2026-06-23, merged to `main`)
New `Contact` model (per-user; optional `companyId` FK; name, email, position, phone, linkedinUrl, notes, followUpDate) + `ApplicationContact` explicit join (unique `[applicationId, contactId]`). New `contacts/` CRUD module (`?search` over name/email, isolation) + `POST/DELETE /api/applications/:id/contacts` link/unlink (duplicate → 409, idempotent unlink, cascade on delete); `GET /api/applications/:id` now includes `contacts`. Spec: `docs/superpowers/specs/2026-06-23-contacts-design.md`; plan: `docs/superpowers/plans/2026-06-23-contacts-backend.md`.

## v2 — Post-Contacts fixes ☑ (2026-06-23, merged to `main`)
- **Auth refresh race:** rotation now uses idempotent `deleteMany` + count guard (concurrent refresh no longer throws Prisma P2025 / 500; racing loser gets a clean 401).
- **Remember me:** login/register accept `rememberMe` (carried in the refresh JWT `rmb` claim, preserved across rotation); checked → 30-day token + persistent cookie, unchecked → 1-day token + session cookie.
- **Application `source`** max raised 200 → 2000 (real job-board URLs).

## v2 — Analytics (backend slice) ☑ (2026-06-23, merged to `main`)
New read-only `analytics/` module: one composite `GET /api/analytics` → `{ metrics, funnel, overTime }`, all `userId`-scoped, no schema change. **Metrics:** `totalApplications`, `interviewRate` (apps with ≥1 interview ÷ total), `offerRate` ((Offer+Accepted) ÷ total), `rejectionRate` (Rejected ÷ total); rates are `0..1`, `0` when total is 0. **Funnel:** count by status, all 9 statuses in canonical order, zero-filled. **overTime:** last 12 months, bucketed by `COALESCE(applicationDate, createdAt)` via a parameterized `date_trunc` raw query, zero-filled. Spec: `docs/superpowers/specs/2026-06-23-analytics-design.md`; plan: `docs/superpowers/plans/2026-06-23-analytics-backend.md`. **Reviewed (read-only subagent) — Approved, no blocking;** non-blocking polish in `86c676b` (commented the `date_trunc` query's UTC/`timestamp`-without-tz dependency; test imports `monthKeys` instead of duplicating it).

## Tests
**170 passing / 1 skipped / 171 total — reliably green** (CI runs the suite against a Postgres service on every push/PR). The old intermittent cross-file flake was **Prisma connection-pool exhaustion** across Jest files — fixed 2026-06-26 by `connection_limit=1` on the test `DATABASE_URL` (CI workflow + `.env.test`), so the serial suite can't run Postgres out of connections. The lone `extract.test.js` PDF case is now **`test.skip`** with an explanation: pdf-parse v2 (modern pdf.js) can't initialize inside jest `--experimental-vm-modules`; the code path is verified in real Node and in the deployed analysis. (Earlier: 156/157 with that case failing.) The OpenRouter resilience round (2026-06-25) added 12 engine tests (error `kind` tagging, abort-during-body, retry/fallback/429-fast-path/Retry-After re-sweep) + a service-level logging assertion. Run serially (`jest --runInBand`) — parallel runs fail on shared-test-DB contention.

## In Flight
_v2 complete; **v3 in progress** — Documents (V3-1) + Activity Log (V3-2) + Résumé Analysis (V3-3) + résumé-analysis **OpenRouter LLM layer (V3-4)**, now hardened with a **retry/model-fallback/Retry-After resilience round** (2026-06-25, `ec19a5e`) and confirmed working live, all merged to `main` (local only). **170 passing / 1 skipped — reliably green in CI** (connection-pool flake fixed). **BE-6 Deploy DONE — API is live** on Render + Neon + Supabase Storage (`DEPLOY.md`, `47260a7`), plus a **portfolio-readiness pass** (demo seed, keep-alive, CI, README), an **AI cover-letter generator** with Save-to-Documents, and **job-posting auto-import** (see Notes). See root `../TRACKER.md` for the full module status._

## Notes / Blockers
- 2026-06-30 — **V3-6 Editor v2 (Typography & Page Layout) — docs only in this repo.** The feature is **frontend-only** (font family/size, color/highlight, paper-style page canvas; page settings stored in the TipTap `content` JSON via a custom `PageDocument` node — no backend/API/migration). This repo only carries the **spec + plan** under `docs/superpowers/` (`2026-06-29-editor-v2-typography-page-layout-*.md`), merged to `main`. Implementation + 170 tests live in `SmartJobSearchCRM-FE`. See the master tracker V3-6 row.
- 2026-06-29 — **V3-5 In-app document editor (BE) done, merged & pushed to GitHub** (PR #1 → `main`, CI-green). New **`authored-documents`** module for rich-text documents authored *in the app* (distinct from the V3-1 *uploaded-file* `documents` module). `AuthoredDocument` model + `AuthoredDocType` enum (`Resume|CoverLetter|Note`) via migration `20260629034452_add_authored_document` (TipTap/ProseMirror JSON in a `content` Json column; optional `applicationId` link, `onDelete: SetNull`; back-relations on `User` + `Application`). Endpoints `GET/POST /api/authored-documents`, `GET/PATCH/DELETE /:id`, all `userId`-scoped; **list omits `content`** (Prisma `select`); ownership enforced on read **and** write (PATCH/DELETE via `getById(userId,id)` pre-check) and `applicationId` ownership checked on create + update; PATCH is the autosave target. Zod `content` schema is `{type:string}.passthrough()` (accepts any TipTap doc, strips body injection like `userId`/`id`). TDD: **13 tests** (incl. cross-user GET/PATCH/DELETE 404 + foreign-`applicationId` 404) → **188 total** (full suite green). Built subagent-driven (fresh implementer + reviewer per task) + a final Opus whole-branch review. Spec: `docs/superpowers/specs/2026-06-29-authored-document-editor-design.md`; plan: `docs/superpowers/plans/2026-06-29-authored-document-editor.md`. Note: the parallel Jest run can occasionally flake with "socket hang up" under DB connection pressure (pre-existing); clean runs are green. Deferred (post-v1): authored-résumé → ATS bridge.
- 2026-06-26 — **Work-mode field on applications** (`a22d1c6`). New `WorkMode` enum (`Remote`/`Hybrid`/`OnSite`) + nullable `application.workMode` column (migration `20260626051249_add_work_mode`, applied to Neon). Added to the create/update Zod schema (services spread `data`, so no service change). The `postings` parser also extracts `workMode` (added to its EXTRACT_SCHEMA + prompt). Demo `seed.js` assigns modes to the demo apps (re-seeded to prod). Applications tests assert workMode persists; postings test asserts it's extracted. **170/171 tests.**
- 2026-06-26 — **Job-posting auto-import + test-DB flake fix** (`86491db`). New **`postings`** module: `POST /api/postings/parse` extracts `{position, companyName, salaryMin, salaryMax, jobDescription, source}` from pasted posting **text or a URL**. Added a generic **`generateJson(messages, schema)`** to `engine/openrouter.js` (reuses the shared `chat()` + `withModelFallback()` resilience — all 21 engine tests still green). Pasted text is returned **verbatim** as the JD; a URL is fetched best-effort (12s timeout, browser UA) behind an **SSRF guard** (`assertPublicUrl` blocks localhost/private/link-local/metadata hosts) with a clear "paste the text instead" message since Indeed/LinkedIn block bots. AI-only: no key/saturated → 503. 5 API tests (parse / no-key-no-call / AI-failure-503 / SSRF-400 / auth). **Verified live** on the demo. **Fixed the serial-suite flake**: it was Prisma connection-pool exhaustion across Jest files (`PrismaClientKnownRequestError` when many suites ran together) — `connection_limit=1` on the test DB URL makes the full `--runInBand` suite reliably pass (**170/171**, was intermittently failing 4–7). **FE↔BE coupling note:** the `postings` service imports the OpenRouter engine from `analysis/engine` (shared AI capability).
- 2026-06-26 — **Documents: accept `text/plain` uploads** (`0f02467`) — for the cover-letter **Save to Documents** flow (FE saves the letter as a `.txt` CoverLetter doc + links it to the application). Widened the multer allow-list in `documents.upload.js`; the disallowed-type test now uses `image/png` and a `.txt` acceptance test was added. **Verified live**: `.txt` upload → 201, link → 201, delete → 204. **165/166 tests.**
- 2026-06-26 — **AI cover-letter generator** (`40b1520`). New `POST /api/analysis/cover-letter` (in the analysis module): fetches the application (incl. `company`) + the chosen résumé, extracts its text, and prompts an LLM for a tailored cover letter. **Refactored `engine/openrouter.js`** to avoid duplicating the resilience logic — extracted `chat()` (one model, one attempt) and a generic **`withModelFallback(attempt)`**; `completeWithFallback` (JSON analysis) and the new `generateTextWithFallback` (freeform text) now share the same retry / 429-fast-fallback / Retry-After re-sweep policy. All 21 existing openrouter tests stayed green through the refactor. **AI-only** (prose has no deterministic fallback): missing JD → `400 ValidationError`, no key / saturated provider → `503 AI_UNAVAILABLE` with a friendly message (logged with the error `kind`). TDD: 4 API tests (success/no-JD/no-key-no-call/AI-failure) via the auto-mocked engine. **Verified live** on the demo data — `gpt-oss-20b:free` returned a coherent, role-specific letter (PDF résumé extraction worked in prod). **164/165 tests.**
- 2026-06-26 — **Portfolio-readiness pass (BE side).** (1) **Demo seed** — `prisma/seed.js` (`npm run seed`, `9532213`): re-runnable, wipes + recreates a public demo user `demo@smartjobsearch.app` with realistic data across the whole pipeline (6 companies, 10 applications across every status, 9 interviews with past results + upcoming, 8 contacts w/ follow-ups, 3 downloadable PDF documents written to storage, 2 precomputed `ResumeAnalysis` rows, a 12-event activity feed). Seeded to **prod** (Neon + Supabase) and verified live via the FE "Try the demo" button. (2) **Keep-alive workflow** (`.github/workflows/keep-alive.yml`) — pings `/api/health` every ~14 min so Render's free tier doesn't cold-start for a reviewer. (3) **CI** (`.github/workflows/ci.yml`, `80ecdea`) — Jest + Supertest against a Postgres service container on push/PR; **green badge** in the README. (4) **README** rewritten as a portfolio piece (live demo, architecture, engineering highlights, full API table). (5) Quarantined the long-standing `extract.test.js` PDF failure (see Tests) so the suite is honestly green. **Open item:** the demo is a shared account — re-run `npm run seed` (with `.env.prod`) to refresh it; a scheduled auto-reseed Action is possible (needs `DATABASE_URL`/`S3_*` repo secrets).
- 2026-06-25 — **V3-4 OpenRouter resilience round (BE) — retry + model fallback + Retry-After re-sweep + diagnostic logging. Merged to `main`** (`ec19a5e`, `--no-ff`). Symptom: still hitting "AI was unavailable". Root cause (surfaced by the new logging, **not a code bug**): the configured free model was **rate-limited upstream (429)**, and the silent `catch {}` hid the reason. Fixes in `engine/openrouter.js`: (1) **diagnostic logging** — `analysis.service.js` replaces the empty catch with `console.warn(kind, model, upstream message)`; (2) **typed `OpenRouterError`** with a `kind` discriminator (`config`/`timeout`/`network`/`http`/`parse`), non-2xx errors now include the response **body**, and aborts during *body read* are correctly tagged `timeout` (fixed a `kind=unknown` bug); (3) **`completeWithFallback`** — `OPENROUTER_MODEL` is now a **comma-separated chain** tried in order: retry genuine glitches (timeout/network/5xx) on the same model with backoff, **jump to the next model immediately on a 429** (no wasted same-model retries), and when the *whole chain* is rate-limited at once, **honor `Retry-After` and re-sweep once** (bounded by `OPENROUTER_RETRY_AFTER_MAX_MS`, default 10s). Deterministic engine stays the authoritative final fallback. **Verified-live model chain** (queried OpenRouter's models API + tested each): `openai/gpt-oss-20b:free → nvidia/nemotron-3-super-120b-a12b:free → qwen/qwen3-next-80b-a3b-instruct:free` — the old `gpt-oss-120b:free` **no longer advertises structured-output support** and was failing. New env tuning vars: `OPENROUTER_ATTEMPTS`, `OPENROUTER_RETRY_BASE_MS`, `OPENROUTER_RETRY_AFTER_MAX_MS` (documented in `.env.example`). **Verified end-to-end in the real UI** — "AI-assisted match · openai/gpt-oss-20b:free" replaces the orange fallback banner; 5/5 live `completeWithFallback` runs succeeded. **BE 156/157 tests.** **Honest caveat:** if every free model stays saturated longer than the 10s cap (rare, peak only), it still falls back to keyword match — for bulletproof AI, add ~$5 OpenRouter credit or set `OPENROUTER_MODEL` to a sub-cent paid model (e.g. `openai/gpt-4o-mini`). **⚠ Restart `node src/server.js`** to load new code + `.env`. **Follow-ups:** rotate the OpenRouter key (it appeared in a session transcript); the pre-existing `extract.test.js` PDF failure is still red (separate issue).
- 2026-06-25 — **V3-4 OpenRouter live-fix round (BE) — now actually works with free models.** Debugged "AI was unavailable" with the user's real key: (1) shipped default model 404'd; (2) free models don't truly enforce `json_schema` → returned prose on real-sized JDs; (3) 15s timeout too tight. Reworked `engine/openrouter.js` to a **prompt-based JSON approach** — forceful JSON-only prompt + `response_format: json_object` + lenient extraction (strip prose/```fences, exported `extractJson`) + Zod, dropping strict `json_schema`/`provider.require_parameters`. Default model → **`openai/gpt-oss-120b:free`**; timeout 15s→40s; tests gained a leak-proof `afterEach` for `OPENROUTER_API_KEY` + a lenient-parse test. Verified end-to-end on the real résumé+JD (score 44; semantic skills like RESTful services/DevOps/Web security/TDD + a real suggestion). Key validated via `GET /api/v1/key`. **142 tests.** **Caveats:** gpt-oss-120b ~26s/run; free models 429 intermittently (re-run); always falls back to deterministic. For instant/reliable AI, set `OPENROUTER_MODEL` to a sub-cent paid model (e.g. `google/gemini-2.0-flash-001`). **⚠ Restart `node src/server.js`** to load this code + the `.env` model (no auto-reload) before the AI toggle works live.
- 2026-06-24 — **V3-4 OpenRouter LLM layer (BE) done & merged** (`9fde925`, `--no-ff`). `engine/openrouter.js`: `complete()` (global `fetch` to OpenRouter chat/completions — temp 0, `response_format: json_schema`, `provider: { require_parameters: true }`, ~15s AbortController timeout, auth header; Zod-validate-or-throw) + `aiMatch()` (maps LLM `{skills,suggestions}` via the existing `weightOf` formula → matcher shape, `source:'ai'`). Service `run({…,useAi})` tries `aiMatch` only when `useAi` + `OPENROUTER_API_KEY` + extraction-ok + JD present, **falls back to deterministic `matchJd` on ANY failure**; records `meta.aiUsed`/`aiModel`. New `GET /api/analysis/config → {aiAvailable}` (mounted before `/:id`); schema widened (`useAi?`, meta ai fields, `source` enum `['rule','ai']`). **No new dependency, no migration.** TDD: 6 openrouter + 4 API = **141 total**; all network mocked (offline). Read-only review clean. **Config env:** `OPENROUTER_API_KEY` (presence ⇒ AI available), `OPENROUTER_MODEL` (default free model, overridable), `OPENROUTER_BASE_URL` — set them + restart to run AI live.
- 2026-06-24 — **V3-3 follow-up fix (keyword noise) merged** (`11c8454`, `--no-ff`). The JD-match engine's salient-token fallback treated the top-15 frequent JD tokens as "hard skills", so generic filler (*frameworks/implement/practices/what/greenfield/…*) polluted matched/missing chips **and** suggestions. Fix: score **only** against the curated dictionary (dropped the fallback) and expanded `engine/skills.json` from ~26 → **115** real skills; suggestions now reference only genuine skills. Regression test asserts filler never appears. BE 131 tests. Tradeoff: dictionary-only is clean but bounded — a skill not in the list isn't detected (motivates the planned V3-4 OpenRouter LLM layer). **Restart the dev server after pulling** (no auto-reload).
- 2026-06-24 — **V3-3 Résumé Analysis / ATS (BE) done & merged** (`c2ba535`, `--no-ff`). New `analysis/` module: a **pure, offline scoring engine** (`engine/extract.js` PDF via pdf-parse v2 / DOCX via mammoth → `{text, ok}`; `engine/ats.js` ATS-friendliness audit → parseability/sections/contactInfo/formatting/length; `engine/match.js` JD-keyword + bundled `skills.json` match → matched/missing + weighted `matchScore`, null on empty JD; `engine/suggestions.js` rule suggestions) + an immutable, Zod-validated `ResumeAnalysis` snapshot (real migration; scores as columns + `report` Json). Endpoints `POST/GET /api/analysis`, `GET/DELETE /:id`, all `userId`-scoped; unparseable résumé → 201 parseability-failure (never 500); no-JD → `matchScore` null + full audit. TDD: 5 match + 4 ats + 3 suggestions + 4 extract + 5 API = 21 → **130 total**. Read-only review clean after fixes (`PDFParse.destroy()` to release the pdfjs worker; exclude multi-word-phrase component tokens from the salient-keyword pass). **Note:** the jest `test` script now sets `NODE_OPTIONS=--experimental-vm-modules` (pdfjs needs a dynamic-import worker); fixtures generated by `tests/fixtures/generate-resume-fixtures.js` (pdf-lib uncompressed + docx).
- 2026-06-24 — **V3-2 Activity Log (BE) done & merged** (`4e2994d`, `--no-ff`). New `activity/` module: an `ActivityLog` table (real migration `…_add_activity_log`; nullable `applicationId` `onDelete: SetNull`; JSON `metadata` snapshots) + a shared `record(userId, action, {applicationId, metadata})` helper called at curated write points across applications/interviews/documents/contacts (status & result events fire only on real transitions; `ApplicationDeleted` logs with `applicationId: null`). One filterable `GET /api/activity` (`applicationId` filter, opaque compound `<iso>|<id>` cursor via `limit`/`before` → `{items, nextCursor}`), `userId`-scoped, `userId` never returned. TDD: 11 activity tests → **108 total**. Read-only review clean; fix applied: compound `(createdAt, id)` cursor + tiebreaker ordering so events sharing a millisecond aren't dropped at a page boundary.
- 2026-06-24 — **V3-1 Documents (BE) done & merged** (`f0f55d8`, `--no-ff`). New `documents/` module: real file upload via **multer** (memory storage, 5 MB + PDF/DOC/DOCX allowlist → 400) behind a swappable `src/shared/storage/` interface (local disk in dev via `UPLOAD_DIR`; S3/Blob adapter deferred). Endpoints: `POST/GET /api/documents`, `GET /api/documents/:id/file` (stream + Content-Disposition), `PATCH`/`DELETE /api/documents/:id`, all `userId`-scoped; `storageKey`/`userId` never returned. `Document` + `ApplicationDocument` models via real migration `20260624010245_add_documents`; `POST/DELETE /api/applications/:id/documents` link/unlink (409 dup, idempotent 204) + `documents[]` on application detail. TDD: 3 storage + 10 documents + 5 application-link tests → **97 total**. Read-only review clean; applied fixes: download stream sets headers on `'open'` (no `ERR_HTTP_HEADERS_SENT` if the file is missing) + non-printable stripped from filename; orphaned blob removed if the DB insert fails after `storage.save`.
- 2026-06-23 — **V2-3 Reminders (BE) done & merged** (`515eebb`, `--no-ff`, branch deleted). New read-only `reminders/` module: composite `GET /api/reminders` → 4 `userId`-scoped buckets over a 7-day window (`interviews.{upcoming,overdue}`, `followUps.{due,upcoming}`) + `counts`, via `dashboard`/`analytics`-style `Promise.all`; no DB migration. Contacts Zod widened (`followUpDate: z.coerce.date().nullable().optional()`) so `PATCH /contacts/:id {followUpDate:null}` clears a follow-up. TDD: 5 new tests (1 contacts null-clear + 4 reminders: auth, empty, bucketing, cross-user isolation) → **79 total**. Read-only subagent review clean (no blockers).
- 2026-06-23 — BE-0…BE-5 implemented (TDD) and **merged to `main`** (`--no-ff`, feature branch deleted). 42/42 tests pass on merged main.
- Reviews: focused auth review (Approved) + whole-branch review ("merge after fixes"). Applied refresh-token family revocation on reuse, expired-token reaping on login, salary cross-field validation, logout userId scoping, deterministic interview ordering.
- 2026-06-22 — Local Postgres published on host port **5434** (5432 occupied by another project).
